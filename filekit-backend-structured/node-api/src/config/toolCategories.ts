// Later you can expand this to drive your routes, docs and UI.
export const toolCategories = {
  pdf: {
    organize: ["merge", "split", "reorder"],
    edit: ["flatten", "watermark", "repair"],
    convert: ["toWord", "toImage", "toPpt"]
  },
  image: {
    basic: ["resize", "convert", "compress"]
  },
  media: {
    video: ["convert", "trim", "merge"],
    audio: ["convert", "extract"]
  }
};
